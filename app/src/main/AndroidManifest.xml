<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.excalibur.followproject">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:name=".application.SkinApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">

        </activity>

        <activity android:name=".activity.zidongfanhui.ZiDongFanHuiActivity">

        </activity>

        <activity android:name=".activity.touming.TouMingZhuangTaiLanActivity">

        </activity>

        <activity android:name=".activity.cardstack.CardStackActivity">

        </activity>

        <activity android:name=".anim.AnimActivity">

        </activity>

        <activity android:name=".activity.skin.SkinActivity">

        </activity>

        <activity android:name=".activity.sync.SyncRecyclerActivity">

        </activity>

        <activity android:name=".activity.bezier.BezierActivity">

        </activity>

        <activity android:name=".view.crul.CurlActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>